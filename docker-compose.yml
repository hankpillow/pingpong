version: '2'
services:
  pingpong:
    image: mrboots/pingpong
    build: .
    volumes:
      - ./URLS:/var/URLS
      - ./log:/var/log:rw
    environment:
      - FOLLOW_LINKS=1
      - APPEND_LOG=1
      - MAX_TIME=30
